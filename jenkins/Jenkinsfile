pipeline{
    agent any
    tools{
        maven 'maven'
    }
    stages{
        stage("clone")
        {
            steps{
                sh 'git clone https://github.com/vPt-King/project-vnpt.git'
            }
        }
        stage("test")
        {
            steps{
                sh "mvn clean test"
            }
        }
        stage("build")
        {
            steps{
                sh "mvn clean install"
            }
        }
        stage("build and push image")
        {
            steps{
                withDockerRegistry(credentialsId: '573f4d52-0eea-43a5-95bc-fa8dda139b5b', url: 'https://index.docker.io/v1/') {
                    sh "docker build -f Dockerfile -t thanhvu638/project-vnpt:${BUILD_ID} ../javaweb/"
                    sh "docker push thanhvu638/project-vnpt:${BUILD_ID}"
                }
            }
        }
        stage("ansible")
        {
            steps{
                sh "/usr/bin/ansible-playbook /var/lib/jenkins/workspace/project-vnpt/ansible/ansible.yml -i /var/lib/jenkins/workspace/project-vnpt/ansible/inventory.ini -e \"version=${BUILD_ID}\""
            }
        }
    }
}